# Cloudflare Pages Headers Configuration
# Enable Cross-Origin Isolation for FFmpeg.wasm support
# Required for SharedArrayBuffer which FFmpeg.wasm needs for MP4 conversion

/export-hybrid-video.html
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Opener-Policy: same-origin

# Also apply to the root export page if accessed directly
/export-hybrid-video
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Opener-Policy: same-origin
