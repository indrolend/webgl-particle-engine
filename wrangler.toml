# Cloudflare Pages configuration for static site deployment
name = "webgl-particle-engine"
compatibility_date = "2026-02-05"

# This is a static site - deploy all files from root as assets
[assets]
directory = "."
# Exclude git and config files
exclude = [".git", ".github", "node_modules", "package-lock.json"]
